option(ENABLE_I18N "" ON)

if (ENABLE_I18N)
    LIST_HEADER(${CMAKE_CURRENT_LIST_DIR} I18N_HEADER)
    LIST_SRC(${CMAKE_CURRENT_LIST_DIR} I18N_SRC)
else()
    set(I18N_HEADER )
    set(I18N_SRC )
endif ()
