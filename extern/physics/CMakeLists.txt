option(ENABLE_PHYSICS "" ON)

if (ENABLE_PHYSICS)
    LIST_HEADER(${CMAKE_CURRENT_LIST_DIR} PHYSICS_HEADER)
    LIST_SRC(${CMAKE_CURRENT_LIST_DIR} PHYSICS_SRC)

    add_definitions(-D ENABLE_PHYSICS)
else()
    set(PHYSICS_HEADER )
    set(PHYSICS_SRC )
endif ()