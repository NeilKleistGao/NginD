set(ANIMATION_PREFIX kernel/animation)

set(ANIMATION_HEADER
        ${ANIMATION_PREFIX}/aseprite.h
        ${ANIMATION_PREFIX}/aseprite_frame.h
        ${ANIMATION_PREFIX}/aseprite_tag.h)
set(ANIMATION_SRC
        ${ANIMATION_PREFIX}/aseprite.cc
        ${ANIMATION_PREFIX}/aseprite_frame.cc
        ${ANIMATION_PREFIX}/aseprite_tag.cc)