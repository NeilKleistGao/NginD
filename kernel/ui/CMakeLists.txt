set(UI_PREFIX kernel/ui)

set(UI_HEADER
        ${UI_PREFIX}/kd_tree.h
        ${UI_PREFIX}/clickable_receiver.h
        ${UI_PREFIX}/event_system.h)

set(UI_SRC
        ${UI_PREFIX}/kd_tree.cc
        ${UI_PREFIX}/event_system.cc)